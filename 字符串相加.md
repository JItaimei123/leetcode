## [字符串相加](#字符串相加)

给定两个字符串形式的非负整数 `num1` 和 `num2`，计算它们的和并同样以字符串形式返回。

你不能使用任何內建的用于处理大整数的库（比如 `BigInteger`），也不能直接将输入的字符串转换为整数形式。

**示例 1：**

```C++
输入：num1 = "11", num2 = "123"
输出："134"
```

**示例 2：**

```C++
输入：num1 = "456", num2 = "77"
输出："533"
```

**示例 3：**

```C++
输入：num1 = "0", num2 = "0"
输出："0"
```

### 整体思路

用 end1 与 end2 作为索引先遍历两个"数组"，**只要还有至少一个数字有未处理的位（索引≥0）就继续**，记录从低位到高位对齐的每一个数的相加后的余数与进位数，定义一个 string 容器存放这一串字符串。由于计算是从低位写起，所以要用到头插操作。

```C++
class Solution 
{
public:
    string addStrings(string num1, string num2) 
    {
        string str;
        int end1 = num1.size() - 1;
        int end2 = num2.size() - 1;
        int next = 0;  // 记录进位
        while (end1 >= 0 || end2 >= 0) 
        {
            int val1 = end1 >= 0 ? num1[end1--] - '0' : 0;
            int val2 = end2 >= 0 ? num2[end2--] - '0' : 0;
            int ret = val1 + val2 + next;
            next = ret / 10;
            ret = ret % 10;
            str.insert(str.begin(), '0' + ret);
        }
        if (next == 1)  // 考虑1+9这种特殊情况
        {
            str.insert(str.begin(), '1');
        }
        return str;
    }
};
```

### 小知识点解剖

```C++
next = ret / 10;
ret = ret % 10;
```

这里顺序不能反，先进位再取余。

```C++
int val1 = end1 >= 0 ? num1[end1--] - '0' : 0;
int val2 = end2 >= 0 ? num2[end2--] - '0' : 0;
```

`num2[end2--] - '0'` 中的 `- '0'` 是为了将数字字符（如 `'5'`）转换为对应的整数值（如 `5`）。

---
