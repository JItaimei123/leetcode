将一个给定字符串 `s` 根据给定的行数 `numRows` ，以从上往下、从左到右进行 Z 字形排列。

比如输入字符串为 `"PAYPALISHIRING"` 行数为 `3` 时，排列如下：

```C++
P   A   H   N
A P L S I I G
Y   I   R
```

之后，你的输出需要从左往右逐行读取，产生出一个新的字符串，比如：`"PAHNAPLSIIGYIR"`。

请你实现这个将字符串进行指定行数变换的函数：

```C++
string convert(string s, int numRows);
```

**示例 1：**

```C++
输入：s = "PAYPALISHIRING", numRows = 3
输出："PAHNAPLSIIGYIR"
```

**示例 2：**

```C++
输入：s = "PAYPALISHIRING", numRows = 4
输出："PINALSIGYAHRPI"
解释：
P     I    N
A   L S  I G
Y A   H R
P     I
```

![image.png](./images/image.png)

这题本质找规律，根据题目模拟，可得上述矩阵中的情况，数组中对应下标在矩阵中的落点如图所示，若按部就班的去遍历复杂度是O(len * n),显然并不理想，所以我们要找出其中存在的规律

1.在第0行`(n = 0)`,与最后一行`(n = n-1)`明显规律是一致的，下一个数是往后推`n-2`个(因为是z字，挪动的时候不会包含首尾)，相邻两个数的公差`d`是`2n-2`

2.在`1~n-2`行也有统一的规律，可以发现这里是两个为一组往后递进的，假设行数是i，那么第一个数一定是`i`，第二个数就是`d-i`，也就是以`(i,d-i)`,公差为d往后递推，`(i,d-i) → (i+d,d-i+d)`,直到其中一位>n停止

```C++
class Solution {
public:
    string convert(string s, int numRows) 
    {
        int n = s.size();
        string ret;
        int d = 2 * numRows -2;
        //处理边界情况
        if(numRows == 1) return s;
        //处理第一行
        for(int i=0;i<n;i+=d)
        ret += s[i];
        //处理中间行
        for(int k = 1;k<numRows-1;k++)
        {
            for(int i = k,j = d-k;i<n || j<n;i+=d,j+=d)
            {
                if(i < n) ret += s[i];
                if(j < n) ret += s[j]; 
            }
        }
        //处理最后一行
        for(int i = numRows-1;i<n;i+=d)
        ret += s[i];
        return ret;
    }
};
```